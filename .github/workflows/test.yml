name: Run Tests

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 🔁 Checkout kode
        uses: actions/checkout@v4

      - name: 🐍 Setup Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"

      - name: 📦 Install dependensi
        run: |
          python -m pip install --upgrade pip
          pip install -r day16/requirements.txt  # ← baca dari folder day16

      - name: 🧪 Jalankan pytest hanya di day16
        run: |
          cd day16
          pytest -v